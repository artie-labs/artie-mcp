[project]
name = "artie-mcp"
version = "0.1.7"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastmcp>=3.4.4",
    "httpx>=0.28.1",
    "pyyaml>=6.0",
]

[dependency-groups]
dev = [
    "ruff==0.15.22",
]
