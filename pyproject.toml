[project]
name = "artie-mcp"
version = "0.1.8"
description = "Reference implementation of Artie's hosted MCP server for managing data pipelines"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastmcp>=3.4.4",
    "httpx>=0.28.1",
    "opentelemetry-exporter-otlp-proto-http>=1.40.0",
    "opentelemetry-sdk>=1.40.0",
    "pyyaml>=6.0",
]

[dependency-groups]
dev = [
    "ruff==0.15.22",
]
